set(x 1)
set(y 3)
message("Outer Scope : x = ${x}, y= ${y}")
block()
    set(x 15 PARENT_SCOPE)
    unset(y PARENT_SCOPE)
    message("Inner Scope : x = ${x}, y= ${y}")
endblock()
message("Outer Scope : x = ${x}, y= ${y}")
# Working with list
message("Working with list : ")
set(my_list 1 2 3 4)
message("original list = ${my_list}")
list(LENGTH my_list list_len)
message("Length of list is ${list_len}")
